# Material

TODO

## Wiederholung

Gruppen: $(G, *)$

- neutrales Element: $e_G * g = g$
- inverses Element: $g^{-1} * g = e_G$
- assoziativ
- Kommutativ

Ringe: $(R, + ,\cdot)$

- $(R, +)$ ist abelsche Gruppe
- $\cdot$ ist assoziativ
- Distributivgesetze

Körper: $(K, +, \cdot)$

- $(K, +)$ ist abelsche Gruppe
- $(K\backslash \{0\}, \cdot)$ ist abelsche Gruppe
- Distributivgesetze

## Reelle Zahlen

Eine Folge heißt konvergent gegen a, falls gilt:

$$
\forall \epsilon \gt 0 \exists N \in \mathbb{N} : |a_n - a| \lt \epsilon \forall n \ge N
$$

Also nach einem festen Index N der Unterschied zwischen 2 Folgengliedern unter einem Schwellwert Epsilon liegt.

Eine Folge heißt Cauchyfolge, falls gilt:

$$
\forall\epsilon \gt 0 \exists N \in \mathbb{N} : |a_n - a_m| \lt
$$

TODO

Das heißt, das bei dieser Folge die Elemente auch immer näher aneinander rücken müssen.

Cauchyfolgen müssen nicht unbedingt einen konvergenten Grenzwert haben

Die Menge der Cauchyfolgen mit Werten in $\mathbb{Q}$ bezeichnen wir mit $Ca_\mathbb{Q}$

$$
(a_n) \sim (b_n) : \Leftrightarrow |a_n - b_n| \rightarrow 0
$$

Die zugehörige Äquivalenzklasse ist:

TODO: abschreiben

Wir definieren die reellen Zahlen als:

$$
\mathbb{R} := Ca_\mathbb{Q}/\sim
$$

$\mathbb{R}$ ist eine Cauchyfolge in dem jede Folge einen Grenzwert hat.

## Komplexe Zahlen

Wir wollen nun $x^2 = -1$ lösen. (Hat in $\mathbb{R}$ keine Lösung).

Dafür definieren wir die Menge $\mathbb{C} = \mathbb{R} \times \mathbb{R}$, d.h. wor netrachten Paare $(a,b)$ mit $a,b \in \mathbb{R}$

Neutrale Elemente:

- Nullelement: (0, 0)
- Einselement: (1, 0)

Jede Zahl $r \in \mathbb{R}$ ist auch in $\mathbb{C}$ darstellbar, nämlich als Paar $(r, 0)$

## Polynomringe

Sei K ein Körper. Ein Polynom mit Koeffizienten in K in der Unbestimmten X ist ein formaler Ausdruck der Form:

$$
\sum_{k = 0}^na_kX^k = a_0 + a_1 X + a_2X^2 + ... + a_nX^n
$$

Die Menge aller Polynome mit Koeffizienten in K in der Unbestimmten X bezeichnen wir mit $K[X]$. Beim Nullpolynom ist jedes $a_k$ Null.

Der Grad eines Polynoms ist definiert durch:

$$
deg(f) := \Bigg\{
\begin{align*}
-\inf, \\
max\{j \in \mathbb{N}|a_j\}
\end{align*}
$$

TODO: definition zuender abscrheiben folie 19

TODO folie 20 abschreiben (Polynomfunktion)

TODO: folie 21: (Diese Funktionen addieren)

**Polynomdivision**

Gegeben sind zwei Polynome mit Grad n und Grad m

1. Gilt $n \lt m$ dann ist $h = 0$ und $r = f$
2. Gilt $n \ge m$ dann setze, $q_n := f$
3. Für $i \ge m$ mache folgendes beginnend mit $i = m$
   1. Setze $h_i := \frac{l(q_i)}{b_m}X^{i-m}$ und $q_{i - 1} := q_i - g \cdot h_i$
4. Dann setze $h := \sum_{i=m}^nh_i$ und $r:= q_{m-1}$

Beispiel:

$f(x) = x^5 + 5x^4 - 3x^3 - 20x^2 + 30x - 9$, $n = deg(f) = 5$

$g(x) = x^2 + 3x - 2$, $m = deg(g) = 2$

1. $n \lt m$ trifft nicht zu
2. $q_5 = x^5 + 5x^4 - 3x^3 - 20x^2 + 30x - 9$
3.

$i = 5$:

$h_5 := \frac{l(q_5)}{b_2}X^{5-2} = \frac{1}{1}X^3 = x^3$

$$
\begin{align*}
q_4 &:= q_5 - g \cdot h_5 \\
&= (x^5 + 5x^4 - 3x^3 - 20x^2 + 30x - 9) - (x^2 + 3x - 2)X^3 \\
&= 2x^4 - x^3 - 20x^2 + 30x - 9
\end{align*}
$$

$i = 4$:

$h_4 := \frac{l(q_4)}{b_2}X^{4-2} = \frac{2}{1}X^2 = 2x^2$

$$
\begin{align*}
q_3 &:= q_4 - g \cdot h_4 \\
&= (2x^4 - x^3 - 20x^2 + 30x - 9) - (x^2 + 3x - 2)2x^2 \\
&= -7x^3 - 16x^2 + 30x - 9
\end{align*}
$$

$i = 3$

$h_3 := \frac{l(q_3)}{b_2}X^{3-2} = \frac{-7}{1}X = -7x$

$$
\begin{align*}
q_2 &:= q_3 - g \cdot h_3 \\
&= (-7x^3 - 16x^2 + 30x - 9) - (x^2 + 3x - 2)\cdot(-7x) \\
&= 5x^2 +16x - 9
\end{align*}
$$

$i = 2$

$h_2 := \frac{l(q_2)}{b_2}X^{2-2} = \frac{5}{1}X = 5$

$$
\begin{align*}
q_1 &:= q_2 - g \cdot h_2 \\
&= (5x^2 +16x - 9) - (x^2 + 3x - 2)5 \\
&= x + 1
\end{align*}
$$

4. $h := h_5 + h_4 + h_3 + h_2 = x^3 + 2x^2 - 7x + 5$, $4 = x + 1$

**Andere Methode**

$(x^5 + 5x^4 - 3x^3 -20x^2 + 30x - 9) : (x^2 + 3x - 2) = x^3 + 2x^2 - 7x + 5$

$-(x^5 + 3x^4 - 2x^3)$

$2x^4 - 3x - 20x^2 + 30x - 9$ (q4)

$-(2x^4 + 6x^3 - 4x^2)$

$-7x^3 -16x^2 + 30x - 9$ (q3)

$-(-7x^2 - 21x^2 + 14x)$

$5x^2 + 16x - 9$ (q2)

$-(5x^2 + 15x - 10)$

$x + 1$ (r)

TODO: besser darstellen

**Nullstellen**

Sei K ein Körper. Ein Element $\lambda \in K$ heißt Nullstelle von f, wenn $f(\lambda) = 0$ gilt.

Dafür darf das Polynom nicht konstant sein.

TODO: proposition 8.14

Ein Polynom mit dem Grad n hat maximal n Nullstellen

## Vektorräume (Kapitel 9)

Sei K ein Körper. Ein Vektorraum V über K ("K-Vektorraum") ist eine Menge V zusammen mit zwei Verknüpfungen:

$$
\begin{align*}
+: V \times V \rightarrow V \\
\cdot : K \times V \rightarrow V
\end{align*}
$$

1. $(V, +)$ ist eine abelsche Gruppe
2. Für alle $\lambda, \mu \in K$ und $v, w \in V$ gilt:
   1. TODO: abschreiben

Die Elemente von K nennen wir dann auch Skalare.

TODO: Folie 4 abschreiben

Skalarmultiplikation ist durch komponentenweise Multiplikation definiert:

$$
\begin{pmatrix}
v_1 \\
v_2 \\
v_3 \\
...
\end{pmatrix}
$$

TODO: Folie 5

Sei K ein Körper und V ein K-Vektorraum. Seiten $\lambda \in K$ und $v \in V$. Dann gelten die folgenden Rechenregeln:

1. $0_K \cdot v = 0v$ (Neutrales Element bzgl. der Addition im Körper)
2. $\lambda \cdot 0_V = 0_V$ (Neutrales Element bzgl der Addition im Vektorraum)
3. $\lambda \cdot v = 0 \Rightarrow \lambda = 0 \lor v = 0$
4. $(-1) \cdot v = -v$

**Untervektorraum**

TODO: Definition 9.3 abschreiben

Sei K ein Körper, V ein K-Vektorraum und $W \subseteq V$ ein Untervektorraum. Dann ist W ebenfalls ein K-Vektorraum.

Beispiel:

$$
W := \Big\{\Big(\frac{}{}
$$

TODO von folie 8 abschreiben

TODO: folie 9 abschreiben

**Linearkombination**

Sei k ein Körper, V ein K-Vektorraum und $(v_1, ..., v_r)$ eine (endliche) Familie von Elementen aus V. Ein Element $v \in V$ heißt **Linearkombination** der Familie, falls es $\lambda_1, ... \lambda_r \in K$ gibt mit:

$$
v = \sum_{k = 1}^r
$$

TODO weiter folie 11 abschreibven

TODO follie 12

**Lineare Abhängigkeit/Unabhängigkeit**

Die Familie heißt linear unabhängig, wenn folgendes gilt:

$$
\lambda_1v_1
$$

TODO: folie 13 weiter

> 2 Vektoren sind linear abhängig voneinander, wenn ein Vektor ein vielfaches des anderen Vektor ist.
>
> $v_1 = \lambda \cdot v_2, \lambda \in \mathbb{R}$

Beispiel:
 
1.

$\lambda_1 \cdot v_1 + \lambda_2 \cdot v_1 = 0$

$\lambda_1 \cdot (1,0) + \lambda_2 \cdot (0, 1) = (0,0)$

$(l1 \cdot 1, l1 \cdot 0) + (l2 \cdot 0, l2 \cdot 1) = (0,0)$

$(l1, 0) + (0, ö2) = (0,0)$

$(l1 + 0, 0 + l2) = (l1, l2) = (0,0)$

Deshalb: $v_1, v_2$ sind linear unabhängig

2.

l1 * w1 + l2 * w2 = 0

l1 * (1,2,3) + l2 * (2,4,6) = (0,0,0)

(l1, 2l1, 3l1) + (2l1, 4l2, 6l2) = (0,0,0)

(l1 + 2l2, 2l1 + 4l2, 3l1 + 6l2) = (0,0,0)

l1 + 2l2 = 0 => l1 = -l2, z.B. l1 = -2, l1 = 1

Deshalb sind die linear abhängigW

Sei K ein Körper, V ein K-Vektorraum und seien $v_1, ...,v_r \in V$. Dann sind die folgenden Aussagen äquivalent:

1. TODO
2. TODO

TODO

**Erzeugendensystem**

TODO

**Basis**

TODO folie 17/18

Beispiel:

1. 
   
$v_1, v_2, v_3, w$ sind lienar unabhängig, da folgende Skalare nicht existieren:

$\lambda_1 \cdot v_1 = v_2$
$\lambda_1 \cdot v_1 = v_3$
$\lambda_1 \cdot v_1 = w$
$\lambda_2 \cdot v_2 = v_3$
$\lambda_2 \cdot v_2 = w$
$\lambda_3 \cdot v_3 = w$

2.

$$
\begin{pmatrix}
r_1 \\
r_2 \\
r_3
\end{pmatrix} = r_3 \cdot v_1 + (r_2 - r_3) \cdot v_2 + (r_1 - (r_2 - r_3)) \cdot v_3
$$

$$
\begin{pmatrix}
2 \\ 3 \\ 5
\end{pmatrix} = 5 \cdot 
\begin{pmatrix}
1 \\ 1 \\ 1    
\end{pmatrix} + (-2) \cdot
\begin{pmatrix}
1 \\ 1 \\ 0    
\end{pmatrix} + 4 \cdot
\begin{pmatrix}
1 \\ 0 \\ 0
\end{pmatrix}
$$

TODO: das ist noch nicht richtig. ^





TODO: Übungsblatt 3 Aufgabe 2